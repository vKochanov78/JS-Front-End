function solve(n, input) {
    let arr=[];
    let output = '';

    for (let i=o; i<n; i++){
        arr.push(i);
    }

    for(let i=arr.length-1; i>=0; i--){
        console.log(arr);
    }
}

solve();
